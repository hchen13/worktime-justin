# Secrets — role-specific Feishu app credentials live in .env, never commit
.env

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Local Claude session state
.claude/scheduled_tasks.lock

# Local PM loop notification state
.agents/state/

# Node
node_modules/

# App shell build artifacts (rebuilt via app/build.sh)
app/dist/
app/.build-tmp/

# Test artifacts (reports are regenerated; keep dir structure via .gitkeep)
tests/reports/*
!tests/reports/.gitkeep

# Rejected sprite exploration archives stay local; only accepted production samples are versioned.
docs/assets/sprites/exploration-*/

# WTJ-20260704-084 TTS full regen: local Kokoro-only repro env + downloaded model weights
# stay local (large; not committed) — only the produced .m4a + manifest/provenance ship.
/ttsenv/
/kokoro_models/
